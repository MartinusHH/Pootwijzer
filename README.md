# Pootwijzer

Huisdierhandboek (leeftijd, voeding, weer, medicatie, rassen).

## Welke versie?

| Pad | Herkomst | Opmerking |
| --- | --- | --- |
| `index.html` (root) | **Gemini Canvas** (aug 2026) | Meest complete UI (React + Tailwind via CDN). Open dit bestand in de browser. |
| `vanilla/` | Claude → GitHub PWA | Offline-first met service worker; bewaard op branch `archive/vanilla-pwa` en in `vanilla/`. |

Niets weggegooid: branch `archive/vanilla-pwa` = exacte vorige GitHub-main.

## Openen
Open `index.html` lokaal in Chrome/Firefox (CDN vereist netwerk voor React/Tailwind).
